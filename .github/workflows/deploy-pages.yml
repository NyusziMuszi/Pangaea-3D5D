name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  # Allow manual re-deploys from the Actions tab.
  workflow_dispatch:

# Grant the token the rights actions/deploy-pages needs (OIDC + Pages write).
permissions:
  contents: read
  pages: write
  id-token: write

# Serialize deploys; let an in-flight publish finish rather than cancelling it.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # enablement:true turns Pages on via the API (needs pages:write, granted
      # above) so the deploy has a site to publish to. Without this the build +
      # artifact upload succeed but deploy-pages fails with a generic
      # "Deployment failed, try again later." because no Pages site exists.
      - uses: actions/configure-pages@v5
        with:
          enablement: true
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      # base must match the repo subpath: served at /Pangaea-3D5D/, not /pangaea/.
      - run: npm run build:web
        env:
          PANGAEA_BASE: /Pangaea-3D5D/
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist-web

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
